# Cog will pick a compatible CUDA/cuDNN stack when `build.gpu: true` is enabled.
# These pins are the first test baseline; adjust after the first Replicate build
# if the target hardware requires a newer Blackwell-specific torch wheel.
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1

# Minimal B200 avatar-commander runtime.
#
# Torch/torchvision/torchaudio come from the NVIDIA PyTorch base image. Hunyuan,
# MMAudio, upscale/RIFE and test-only packages are intentionally not installed
# in this image; those run on the RTX media/finalizer worker.

opencv-python==4.11.0.86
kornia==0.8.2
basicsr==1.4.2
facexlib==0.3.0
gfpgan==1.3.8
realesrgan==0.3.0
diffusers==0.37.1
transformers==4.51.3
tokenizers==0.21.4
accelerate==1.13.0
peft==0.17.1
tqdm==4.67.3
einops==0.8.1
imageio[ffmpeg]==2.37.3
easydict==1.13
ftfy==6.3.1
dashscope==1.25.16
imageio-ffmpeg==0.6.0
numpy==1.26.4
omegaconf==2.3.0
hydra-core==1.3.2
lightning==2.6.1
gdown==5.2.0
wget==3.2
librosa==0.11.0
soundfile==0.13.1
decord==0.6.0
GitPython==3.1.46
huggingface_hub[hf_transfer]==0.36.2
orjson==3.11.8
httpx==0.28.1
aiohttp==3.13.3
livekit==1.1.2
supabase==2.28.0
